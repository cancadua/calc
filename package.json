{
  "name": "calc",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "android": "SET NODE_OPTIONS=--openssl-legacy-provider && react-native run-android",
    "ios": "SET NODE_OPTIONS=--openssl-legacy-provider && react-native run-ios",
    "start": "SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts start",
    "build": "SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts build",
    "test": "SET NODE_OPTIONS=--openssl-legacy-provider && jest",
    "lint": "SET NODE_OPTIONS=--openssl-legacy-provider && eslint ."
  },
  "dependencies": {
    "lodash": "^4.17.21",
    "math-expression-evaluator": "^1.3.8",
    "react": "17.0.2",
    "react-native": "0.66.1",
    "react-native-splash-screen": "^3.2.0"
  },
  "devDependencies": {
    "@babel/core": "^7.16.0",
    "@babel/runtime": "^7.16.0",
    "@react-native-community/eslint-config": "^3.0.1",
    "babel-jest": "^27.3.1",
    "eslint": "^8.1.0",
    "jest": "^27.3.1",
    "metro-react-native-babel-preset": "^0.66.2",
    "react-test-renderer": "17.0.2"
  },
  "jest": {
    "preset": "react-native"
  }
}
